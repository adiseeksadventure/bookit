# Backend build context (./server). Deps and build output are produced inside
# the image; secrets are injected at runtime by compose, never baked in.
node_modules
dist
.git
npm-debug.log*
.env
.env.*
!.env.example
Dockerfile
.dockerignore
README.md
.DS_Store
*.tsbuildinfo
